export const SECTION_KEYS = ['dead', 'duplicates', 'stale'];

export const STATUS_TOOLTIPS = {
  'dead': 'Site unreachable',
  'cert-error': 'Certificate issue detected',
  'no-https': 'HTTPS unavailable (HTTP only)'
};

export const STATUS_LABELS = {
  'dead': 'Dead link',
  'cert-error': 'Certificate issue',
  'no-https': 'HTTP only'
};
